<#-- To render the description of a license file header.
    Available context :
    - project the maven project e.g., ${project.property}
    - addSvnKeyWords
    - organizationName
    - projectName
    - inceptionYear
    - file current file to treat
-->
${projectName} - ${project.description}
-> ${project.url}